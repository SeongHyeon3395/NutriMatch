# Copy to .env and fill values. Do NOT commit your .env.
# Backend base URL (no trailing slash)
BASE_URL=http://10.0.2.2:8080
# Supabase anon key (if calling Edge Functions directly)
SUPABASE_ANON_KEY=
